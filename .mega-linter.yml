# MegaLinter configuration
# https://megalinter.io/latest/configuration/

# REPOSITORY_GRYPE currently fails in CI with a broken/incompatible cached
# vulnerability database ("failed to hydrate database: ... UNIQUE constraint
# failed" / "failed to load vulnerability db: database does not exist"),
# which is an environment/tooling issue unrelated to this repository's code.
# Disable it until the upstream grype-db image or cache is fixed.
DISABLE_LINTERS:
  - REPOSITORY_GRYPE
